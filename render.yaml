services:
  - type: web
    name: quickhr
    runtime: python
    plan: free
    buildCommand: ./build.sh
    startCommand: ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.7"
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: quickhr-db
          property: connectionString

databases:
  - name: quickhr-db
    plan: free
